apiVersion: v1
kind: Service
metadata:
  name: dotnet-app-service
spec:
  selector:
    app: dotnet-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  - protocol: TCP
    port: 5001
    targetPort: 5001
  type: LoadBalancer